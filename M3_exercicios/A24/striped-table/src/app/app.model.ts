export class User {
    first: string;
    last: string;
    handle: string;

    constructor(first: string, last: string, handle: string) {
        this.first = first;
        this.last = last;
        this.handle = handle;
    }
}